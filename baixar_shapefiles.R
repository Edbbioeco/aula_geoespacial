# Instalar os pacotes ----

if(require(c("geobr", "tidyverse", "sf"))) {

  install.packages(c("geobr", "tidyverse", "sf"))
}

# Carregar os pacotes ----

library(geobr)

library(tidyverse)

library(sf)
